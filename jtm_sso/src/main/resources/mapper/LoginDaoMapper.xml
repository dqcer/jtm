<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dqcer.jtm.sso.dao.LoginDao">

    <select id="getInfo" resultType="com.alibaba.fastjson.JSONObject">
        SELECT
            pu.id AS id,
            pu.user_name AS username,
            pu.password AS password
        FROM
            pub_user pu
        WHERE
            pu.user_name = #{userName} AND pu.password = #{password}
    </select>

</mapper>